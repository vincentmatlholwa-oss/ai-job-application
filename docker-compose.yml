version: '3.8'

services:
  app:
    build: .
    ports:
      - "3009:3009"
    environment:
      - NODE_ENV=production
      - PORT=3009
      - OPENAI_API_KEY=${OPENAI_API_KEY}
    restart: unless-stopped
